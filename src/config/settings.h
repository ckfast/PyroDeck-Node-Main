#pragma once

#include <Arduino.h>

const char* NODE_FIRMWARE_VERSION = "0.0.1";

const uint16_t HEARTBEAT_INTERVAL = 1000;

const uint8_t OLED_REFRESH_RATE = 100;

const uint32_t PROTOCOL_BAUD = 9600;